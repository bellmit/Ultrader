
import * as ACTION_TYPES from 'actions/Monitors/GreetingMonitorActions';

const initialState = {
};

const greetingMonitor = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default greetingMonitor;