
import * as ACTION_TYPES from 'actions/Rules/AddRuleActions';

const initialState = {
};

const addRule = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default addRule;